name: Deploy Application
on: push
jobs:
  lint_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run lint script
        run: npm run lint
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run test script
        run: npm run test
  deploy_job:
    needs: test_job
    runs-on: ubuntu-latest
    steps:
      - name: Deploy application
        run: npm run build